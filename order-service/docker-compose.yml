services:
  order-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: order-service
    environment:
      PRODUCT_SERVICE_URL: http://host.docker.internal:8081
    ports:
      - "8082:8082"
    networks:
      - observability-net

networks:
  observability-net:
    driver: bridge
