spring:
  datasource:
    url: jdbc:h2:mem:order_test_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

management:
  otlp:
    metrics:
      export:
        enabled: false
